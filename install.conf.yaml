- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.gitconfig: gitconfig
    ~/.inputrc: inputrc
    ~/.vim: vim/
    ~/.zshrc: zshrc
    ~/.ctags: ctags

- shell:
  - [git submodule update --init --recursive, Installing submodules]
